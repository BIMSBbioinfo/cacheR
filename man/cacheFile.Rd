% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cacheFile.R
\name{cacheFile}
\alias{cacheFile}
\title{Create a disk-backed caching decorator for a function}
\usage{
cacheFile(
  cache_dir = NULL,
  backend = getOption("cacheR.backend", "rds"),
  file_args = NULL
)
}
\arguments{
\item{cache_dir}{Directory where cache files will be stored.}

\item{...}{Additional options controlling cache behavior (e.g. hashing,
compression, invalidation rules).}
}
\value{
A decorator object that can be combined with a function using
\verb{\%@\%}, returning a cached version of that function.
}
\description{
\code{cacheFile()} creates a decorator that caches function results to files
in \code{cache_dir}, keyed by the function body and arguments. It also records
parent-child relationships between cached calls so that a "cache tree"
can be reconstructed.
}
\details{
Typical usage (with the \verb{\%@\%} decorator operator) is:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{inner_fun <- cacheFile(cache_dir) \%@\% function(x) x + 1
outer_fun <- cacheFile(cache_dir) \%@\% function(x) inner_fun(x) * 2
}\if{html}{\out{</div>}}
}
