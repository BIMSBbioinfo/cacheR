# Return cache tree nodes

Returns a list/data.frame describing all nodes currently recorded in the
cache tree (each cached call instance).

## Usage

``` r
cacheTree_nodes()
```

## Value

An object representing nodes (e.g. a data.frame with `id`, `fn_label`,
`key`, `cache_file`, etc.).
